# ===============================================
# GAME STORE DXP - BACKEND ENVIRONMENT
# ===============================================
# Copy this file to .env and fill in your actual values

# -----------------------------------------------
# SERVER CONFIGURATION
# -----------------------------------------------
# Port the server will run on
PORT=4000

# Node environment (development, production, test)
NODE_ENV=development

# -----------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------
# MongoDB connection string
# Local: mongodb://localhost:27017/gamestore-dxp
# Atlas: mongodb+srv://<username>:<password>@cluster.mongodb.net/gamestore-dxp
MONGODB_URI=mongodb://localhost:27017/gamestore-dxp

# Database name (if not specified in URI)
DB_NAME=gamestore-dxp

# -----------------------------------------------
# JWT AUTHENTICATION
# -----------------------------------------------
# Secret key for signing JWT tokens (CHANGE THIS IN PRODUCTION!)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# JWT token expiration time
# Examples: '1h', '7d', '30d', '90d'
JWT_EXPIRES_IN=7d

# Cookie settings
COOKIE_HTTPONLY=true
COOKIE_SECURE=false
COOKIE_SAME_SITE=lax

# -----------------------------------------------
# CONTENTSTACK CONFIGURATION (Backend Proxy)
# -----------------------------------------------
# If your backend proxies Contentstack requests

# Your Stack API Key
CONTENTSTACK_API_KEY=your_api_key_here

# Delivery Token for fetching content
CONTENTSTACK_DELIVERY_TOKEN=your_delivery_token_here

# Environment name
CONTENTSTACK_ENVIRONMENT=production

# Region ('us' or 'eu')
CONTENTSTACK_REGION=us

# -----------------------------------------------
# CORS CONFIGURATION
# -----------------------------------------------
# Allowed origins for CORS (comma-separated for multiple)
# Development: http://localhost:5173
# Production: https://yourdomain.com
CORS_ORIGIN=http://localhost:5173

# Allow credentials (cookies, authorization headers)
CORS_CREDENTIALS=true

# -----------------------------------------------
# SECURITY & RATE LIMITING
# -----------------------------------------------
# Bcrypt salt rounds (10-12 recommended)
BCRYPT_SALT_ROUNDS=10

# Rate limiting (requests per window)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# -----------------------------------------------
# LOGGING
# -----------------------------------------------
# Log level (error, warn, info, debug, verbose)
LOG_LEVEL=info

# Enable request logging
LOG_REQUESTS=true

